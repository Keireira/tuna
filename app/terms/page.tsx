import { TermsPage } from '@views';

import type { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Terms of Use',
	description: 'Terms of Use for Uha — a subscription manager for iOS.'
};

export default TermsPage;
